# 前端开发环境 Dockerfile - 支持 Vite 热重载
FROM node:18-alpine

# 安装开发工具
RUN apk add --no-cache curl

# 设置工作目录
WORKDIR /app

# 复制 package 文件
COPY frontend/package*.json ./

# 安装依赖
RUN npm ci

# 暴露端口
EXPOSE 5173

# 设置环境变量
ENV NODE_ENV=development
ENV VITE_HOST=0.0.0.0
ENV VITE_PORT=5173

# 默认命令 (可以被 docker-compose 覆盖)
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]